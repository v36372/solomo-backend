.col-md-8.col-md-offset-2.col-sm-8.col-sm-offset-2.top-10
  = render 'stores/registrations/process', step: 0
  - if current_user.store.feedback_content.present?
    .panel.panel-warning
      .panel-heading
        We have some feedback for you
      .panel-body
        = current_user.store.feedback_content
  - else
    .panel.panel-default
      .panel-body
        Please fill in your true information about your store. Our staff will verify based on your information including email confirmation, sms confmration etc.
  #store_registration
    = form_for current_user, url: stores_registrations_path, method: :patch do |f|
      = render "stores/registrations/form", f: f
